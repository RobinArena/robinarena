{
  "name": "robinshark-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "encore check \"\"",
    "dev": "encore run --port=${ENCORE_LOCAL_PORT:-4000} --browser=never",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "encore.dev": "^1.57.11"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "~5.8.3"
  }
}